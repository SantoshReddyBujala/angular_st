import { FirstDirectDirective } from './first-direct.directive';

describe('FirstDirectDirective', () => {
  it('should create an instance', () => {
    const directive = new FirstDirectDirective();
    expect(directive).toBeTruthy();
  });
});
